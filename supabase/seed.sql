-- MomDaily / optional seed helper
-- Pair creation normally calls public.seed_default_habits(pair_id).
-- For a local Supabase demo, create a pair through the app or replace the UUID below.

-- select public.seed_default_habits('00000000-0000-0000-0000-000000000000'::uuid);

